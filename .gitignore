/target
# `uv sync` writes one; the root package is a library, so it is not pinned.
/uv.lock
/.venv
/python/vinerylab/*.so
__pycache__/

*.local.md
*.local.json

# What the viewer's save key and `dump_scene` write.
/scene.json
/scene.usd
/scene.usda
/scene.usdc
